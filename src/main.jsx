import React from 'react';
import ReactDOM from 'react-dom/client';
import RenkiCash from './RenkiCash.jsx';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <RenkiCash />
  </React.StrictMode>
);
